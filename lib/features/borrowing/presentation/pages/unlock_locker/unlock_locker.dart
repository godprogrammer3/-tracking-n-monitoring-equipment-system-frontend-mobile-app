import 'package:flutter/material.dart';

class UnlockLockerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Unlock Locker'),
      ),
      body: const Center(
        child: Text('Unlock Locker Page'),
      ),
    );
  }
}
